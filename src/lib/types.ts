export type optionList = {
    elevationDataUrl: string;
    textureUrl: string;
    title: string;
};

export type sliderMetrics = {
    bar: number;
    container: number;
    left: number
    getHidden: () => number;
}
